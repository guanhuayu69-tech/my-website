# Lovable 实施指令｜普健堂中医官网 v3

请在现有项目 **PJT TCM Care / 普健堂中医官网** 中应用随附升级源码，保持现有 Lovable Cloud、DeepSeek Secret 和公开域名不变。

## 绝对保留

1. `DEEPSEEK_API_KEY` 继续只存在于服务端 Secret，禁止写入前端、聊天回复或日志。
2. 保留现有 `/api/chat` 或等价 Edge Function 的服务端调用。
3. 每位访客最多发送 8 条消息；第 8 条必须输出六段式阶段总结并锁定输入。
4. 保留 120 急症分流、隐私确认、跨站请求保护、长度限制、安全错误提示。
5. 保留微信 `dannyyuguanhua`、电话 `18022742282`、地址和门诊时间。
6. 保留五张现有品牌图片，不用占位图替换，不重做正式 Logo。

## 应用内容

- 使用升级包中的 `site/index.html`、`site/styles.css`、`site/site.js` 作为前端设计与交互依据。
- 首屏后加入三路“初次来访”任务分流。
- 六类调理方向和三类健康笔记可把对应问题预填至咨询输入。
- 所有服务卡箭头必须成为真实可访问链接。
- 桌面显示右侧快捷栏；900px 以下切换为底部三栏。
- 增加地址、营业时间与全局复制微信反馈。
- 原“用户见证”改为“完整听见、风险优先、明确下一步”的服务结果。
- 增加 sticky header、滚动进度、当前导航、进入视口动效与 reduced-motion 降级。
- 增加 canonical、Open Graph、Twitter Card 和 MedicalClinic JSON-LD。

## 禁止事项

- 不得更改 DeepSeek Key 或把 Key 发到前端。
- 不得添加未经证实的医生姓名、职称、从业年限、疗效、用户案例或资质数字。
- 不得新增无法完成的在线挂号、支付或排班承诺。
- 不得用 3D、鼠标追踪、磁吸按钮或长时间加载动画替代正常导航。
- 不得删除手机端、键盘和低动效降级。

## 发布前验收

1. 1440px 与 390×844 两种视口检查无横向溢出。
2. 点击六个调理方向后，咨询输入显示正确且可编辑的预填文本。
3. 未勾选隐私时不能发送；失败请求不计入 8 次。
4. 第 8 次回复生成阶段总结，输入锁定，微信与电话入口可用。
5. 电话链接为 `tel:18022742282`；复制按钮复制 `dannyyuguanhua`。
6. 页面源码中搜索不到 DeepSeek Key。
7. 所有锚点、键盘焦点、返回和低动效状态正常。
8. 完成后只生成预览，不自动更换域名；公开发布前等待最终确认。

